using System;class Program
{
    static void Main()
    {
        int i = 0;
        string pass = "123";
        string senha ="";
        while (i < 3 && senha != pass)
        {    
            Console.Write("\nEscreva a senha: ");
            senha = Console.ReadLine();
            i++;
        }
        Console.WriteLine(senha == pass ? "Você acertou a senha." : "Você não acertou a senha.");
    }
}
